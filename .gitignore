# macOS
.DS_Store

# Xcode user/local files
xcuserdata/
*.xcuserstate
*.xcuserdatad
DerivedData/
build/

# Swift Package Manager
.swiftpm/
.build/

# Secrets / local config
.env
.env.*
*.xcconfig
!*.example.xcconfig
*.local
*.secret
Secrets.plist
GoogleService-Info.plist

# Supabase local junk
supabase/.branches/
supabase/.temp/
supabase/.env
supabase/.env.*

# DB data snapshots — contain real user PII (names, App Store transaction IDs).
# Keep local-only; never commit to git history.
db-backups/

# AI tooling
.claude/
CLAUDE.md
.agents/
.codex/
AGENTS.md
skills-lock.json

# App Store screenshot generator (Next.js project, not part of the app)
app-store-screenshots/

# Local screenshot exports
finals1/
